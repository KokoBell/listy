interface itemProps {
    id?: number;
    name: string,
    price: number,
    quantity?: number,
    checked?: boolean,
    store_name?: string,
    notes?: string,
    deleted?: boolean
  }

export default itemProps